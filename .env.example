# Database
DATABASE_URL="postgresql://mapestate:mapestate_pass@localhost:5436/mapestate_db?schema=public"
DB_USER=mapestate
DB_PASSWORD=change-this-strong-password
DB_NAME=mapestate_db

# JWT — generate with: openssl rand -base64 48
JWT_SECRET="change-this-in-production"
JWT_EXPIRATION="1d"
JWT_REFRESH_EXPIRATION="7d"

# API
API_PORT=3000

# Frontend
FRONTEND_URL=https://mapestate.ro
VITE_API_URL=/api
VITE_MAPBOX_TOKEN=pk.your-mapbox-token-here

# Email (Resend)
RESEND_API_KEY=re_your_resend_api_key
RESEND_FROM_EMAIL=noreply@mapestate.ro

# Mapbox (server-side, for PDF map images)
MAPBOX_TOKEN=pk.your-mapbox-token-here

# Deploy
APP_PORT=80
